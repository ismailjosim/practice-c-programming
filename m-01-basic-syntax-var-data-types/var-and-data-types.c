#include <stdio.h>

//int name = 10; // int type data take 4 bytes store from (RAM) memory. Here equal(=) sign means assignment operator in Programming and 10 is the value of name.


int main()
{
    int integerValue = 10;
    float floatValue = 10.52;
    char name = 'I';
    printf("%0.2f",floatValue); // print 2 digit in float number.%0.2f % then o.2 means how many digit we want to get and f means float.
    return 0;
}
